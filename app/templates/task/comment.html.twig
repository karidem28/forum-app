{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_task_comment_create'|trans({'%id%': task.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_task_comment_create'|trans({'%id%': task.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'POST', action: url('task_comment', {id: task.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('task_show', {id: task.id}) }}" class="btn btn-link">
            {{ 'action_back_to_task'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}